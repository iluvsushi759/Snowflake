name: Snowflake CI/CD

on:
  push:
    branches:
      - dev    # CI for staging
      - main   # CD for production after PR merge
  workflow_dispatch:  # optional manual trigger

env:
  SQL_DIR: SnowflakeToSnowflake/sql
  TARGET_SCHEMA_DEV: RESEARCH_LANDING
  TARGET_SCHEMA_PROD: RESEARCH_PROD

jobs:
  # ------------------------
  # CI: Staging
  # ------------------------
  ci_dev:
    name: CI - Staging
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install sqlfluff snowflake-connector-python
          pip check

      - name: Debug SQL directory visibility
        run: |
          echo "🔍 Checking contents of ${SQL_DIR}"
          ls -l "${SQL_DIR}" || echo "::error::Directory not found: ${SQL_DIR}"

      - name: Lint SQL files
        run: |
          source venv/bin/activate
          sqlfluff lint "${SQL_DIR}/" --dialect snowflake || true

      - name: Run SQL scripts in Staging
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          TARGET_SCHEMA: ${{ env.TARGET_SCHEMA_DEV }}
          SQL_DIR: ${{ env.SQL_DIR }}
        run: |
          source venv/bin/activate
          python - <<'PY'
          import os, snowflake.connector

          conn = snowflake.connector.connect(
              user=os.environ['SNOWFLAKE_USER'],
              password=os.environ['SNOWFLAKE_PASSWORD'],
              account=os.environ['SNOWFLAKE_ACCOUNT'],
              warehouse=os.environ['SNOWFLAKE_WAREHOUSE'],
              database=os.environ['SNOWFLAKE_DATABASE'],
              role=os.environ.get('SNOWFLAKE_ROLE')
          )
          cur = conn.cursor()
          cur.execute(f"USE SCHEMA {os.environ['TARGET_SCHEMA']}")
          sql_dir = os.environ['SQL_DIR']
          ordered_files = ["01_create_tables.sql", "02_seed_data.sql", "03_transform.sql", "04_data_quality_checks.sql"]

          for file in ordered_files:
              path = f"{sql_dir}/{file}"
              if os.path.isfile(path):
                  print(f"🚀 Running {file} in {os.environ['TARGET_SCHEMA']}")
                  with open(path, 'r') as f:
                      sql = f.read()
                      for stmt in sql.split(';'):
                          if stmt.strip():
                              cur.execute(stmt)
              else:
                  print(f"⚠️ Skipping {file} (not found)")

          cur.close()
          conn.close()
          PY

  # ------------------------
  # CD: Production
  # ------------------------
  cd_prod:
    name: CD - Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: ci_dev
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install snowflake-connector-python

      - name: Run SQL scripts in Production
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          TARGET_SCHEMA: ${{ env.TARGET_SCHEMA_PROD }}
          SQL_DIR: ${{ env.SQL_DIR }}
        run: |
          source venv/bin/activate
          python - <<'PY'
          import os, snowflake.connector

          conn = snowflake.connector.connect(
              user=os.environ['SNOWFLAKE_USER'],
              password=os.environ['SNOWFLAKE_PASSWORD'],
              account=os.environ['SNOWFLAKE_ACCOUNT'],
              warehouse=os.environ['SNOWFLAKE_WAREHOUSE'],
              database=os.environ['SNOWFLAKE_DATABASE'],
              role=os.environ.get('SNOWFLAKE_ROLE')
          )
          cur = conn.cursor()
          cur.execute(f"USE SCHEMA {os.environ['TARGET_SCHEMA']}")
          sql_dir = os.environ['SQL_DIR']
          ordered_files = ["01_create_tables.sql", "02_seed_data.sql", "03_transform.sql", "04_data_quality_checks.sql"]

          for file in ordered_files:
              path = f"{sql_dir}/{file}"
              if os.path.isfile(path):
                  print(f"🚀 Running {file} in {os.environ['TARGET_SCHEMA']}")
                  with open(path, 'r') as f:
                      sql = f.read()
                      for stmt in sql.split(';'):
                          if stmt.strip():
                              cur.execute(stmt)
              else:
                  print(f"⚠️ Skipping {file} (not found)")

          cur.close()
          conn.close()
          PY
